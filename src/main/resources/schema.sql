CREATE TABLE U3_Entity (
    st_ue_code VARCHAR(255) PRIMARY KEY,    -- Company Code
    st_ue_name VARCHAR(255),                 -- Company Name
    st_ue_country_code VARCHAR(50),          -- Country
    st_ue_region_code VARCHAR(50),           -- Region
    st_ue_status_code VARCHAR(50),           -- Status
    st_ue_category_code VARCHAR(50)          -- Category
);
